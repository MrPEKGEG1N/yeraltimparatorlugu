[build]
builder = "nixpacks"

[deploy]
startCommand = "npm start"
healthcheckPath = "/api/health"
restartPolicyType = "on_failure"

# Kalici SQLite: Railway panelinde Volume mount /app/db olmali (canli ortam bu yolu kullaniyor)
[[deploy.volumes]]
mountPath = "/app/db"
