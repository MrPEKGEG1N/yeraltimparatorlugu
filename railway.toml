[build]
builder = "nixpacks"

[deploy]
startCommand = "npm start"
healthcheckPath = "/api/health"
restartPolicyType = "on_failure"

# Kalici SQLite — volume Railway panelinde de bu isimle baglanmali
[[deploy.mounts]]
volumeName = "yeralti-db"
mountPath = "/data"
